

## How to run

Open a terminal inside the `src` folder.

Compile:

```bash
javac *.java
```

Run the simulator first:

```bash
java GamesSimulator
```

This creates:

```text
matches.csv
```

Then run the analysis:

```bash
java TestMatchesAnalysis
```

